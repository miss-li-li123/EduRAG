# 本地部署说明（Windows）

## 服务依赖（Docker）
- MySQL 8：复用本机容器 `mysql`（root / Atguigu.123，端口 3306，库 `subjects_kg`）
- Milvus + Redis + etcd + MinIO：`docker/milvus_redis/docker-compose.yml`（端口 19530 / 6379）
- 所有容器已设为 `unless-stopped`，启动 Docker Desktop 后自动拉起

```bash
cd docker/milvus_redis
docker compose up -d
```

## Python 环境
- 虚拟环境：`.venv`（Python 3.11，torch 为 CPU 版）
- 依赖：`requirements-windows.txt`（已安装）

## 本地模型（ModelScope 下载，无需联网 HF）
- `rag_qa/models/bge-m3`：BGE-M3 稠密+稀疏向量
- `rag_qa/models/bge-reranker-large`：重排序
- `rag_qa/models/bert-base-chinese`：意图分类微调基座
- `rag_qa/core/bert_query_classifier`：意图二分类微调产物（验证集 200/200）

> 基座与微调产物分目录存放，否则 Windows 下 `save_pretrained` 覆盖被自身 mmap 锁定的权重会报错（code 1224）。

重新下载/训练/建库：
```bash
.venv/Scripts/python scripts/setup_01b_resume_models.py   # 下载模型
.venv/Scripts/python scripts/setup_02_import_mysql.py      # 导入高频问答 CSV
.venv/Scripts/python scripts/setup_03_train_intent.py      # 训练意图模型
.venv/Scripts/python scripts/setup_04_build_index.py       # 重建 Milvus 索引
```

## 启动
```bash
# 方式一：双击 run_web.bat
# 方式二：
.venv/Scripts/python -m uvicorn app:app --host 0.0.0.0 --port 8001
```
浏览器访问 http://localhost:8001

## 配置（config.ini）
- LLM：DeepSeek OpenAI 兼容接口
  - base_url = https://api.deepseek.com
  - model = deepseek-flash（可改 deepseek-v4-pro）
  - key 写在 `[llm] dashscope_api_key`（也可用环境变量 ALIYUN_API_KEY 覆盖）

## 已修复的代码问题
1. `base/config.py`：硬编码 D 盘路径改为项目相对路径
2. `rag_qa/core/vector_store.py`：`hybridd_search` 拼写错误 → `hybrid_search`
3. `mysql_qa/db/mysql_client.py`：jpkb 表 answer 列 VARCHAR(1000) 装不下答案 → MEDIUMTEXT
4. docker-compose：etcd/minio 补充 restart 策略
