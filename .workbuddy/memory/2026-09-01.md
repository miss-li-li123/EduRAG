# 2026-09-01

## 项目文档生成
- 浏览了 Itcast_qa_system 全项目（main.py/app.py/rag_qa/core/mysql_qa 等），在**项目根目录新建 `docs/`** 并生成 8 份文档：
  - `docs/README.md`（导航）、`01-项目概述`、`02-系统架构`（含 Mermaid 图）、`03-模块说明`、`04-快速开始与部署`、`05-API接口文档`、`06-意图识别对比实验`、`07-目录结构与文件清单`
  - 同时把根目录原本为空的 `README.md` 改写为指向 `docs/` 的简介。
- 关键事实（写文档时确认）：`rag_qa/__init__.py` 导出的是 `new_rag_system.RAGSystem`（流式+历史，主链路）；`core/rag_system.py` 为旧版参考；`import rag_qa` 会连带加载 `milvus_model` 重依赖。LLM Key 走环境变量 `ALIYUN_API_KEY`（`config.ini` 中留空）。
